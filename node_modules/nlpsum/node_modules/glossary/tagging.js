
module.exports = {
   tokenize: tokenize
}

var termSpec = /([^a-zA-Z]*)([a-zA-Z-\.]*[a-zA-Z])([^a-zA-Z]*[a-zA-Z]*)/


function tokenize(text) {
   var terms = [];
   text.split(/\s+/).forEach(function(word) {
      
   });
   return terms;
}